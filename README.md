# Nova AI Finance — Profile Module (Frontend)

React + TypeScript + Tailwind implementation of the **Profile Management** page,
styled to match the reference dashboard UI (sky-blue/lavender glassmorphism)
and wired to your real Spring Boot `ProfileController` — no mock/static data.

## 1. Folder structure